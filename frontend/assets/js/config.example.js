// Copier ce fichier en config.js si votre frontend utilise une configuration séparée.
// En local : http://127.0.0.1:8000
// En production : remplacer par l’URL réelle du backend.

window.APP_CONFIG = {
  API_BASE_URL: "http://127.0.0.1:8000"
};
